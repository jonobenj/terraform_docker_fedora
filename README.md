Fedora Docker Environment using Terraform
=========================================

This uses terraform to create a fedora docker container to test against.

#### Fedora Version
* latest

#### How to run

`terraform init`
`terraform apply`
